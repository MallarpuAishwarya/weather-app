{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run Weather App",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/weather_app.py",
            "console": "integratedTerminal",
            "cwd": "${workspaceFolder}"
        }
    ]
}